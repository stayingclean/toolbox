import * as THREE from 'three';
import {OrbitControls} from './vendor/OrbitControls.js';
import {GLTFLoader} from './vendor/GLTFLoader.js';
const status=document.querySelector('#status'),view=document.querySelector('#view');
try {
const renderer=new THREE.WebGLRenderer({antialias:true,alpha:false});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);renderer.setClearColor(0xedf0eb);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;view.appendChild(renderer.domElement);
const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(40,innerWidth/innerHeight,.05,1000),controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.maxDistance=140;controls.minDistance=1;
scene.add(new THREE.HemisphereLight(0xffffff,0x777e71,2.5));const sun=new THREE.DirectionalLight(0xfff4e4,3);sun.position.set(20,40,30);scene.add(sun);const fill=new THREE.DirectionalLight(0xe2edff,1.5);fill.position.set(-25,15,-15);scene.add(fill);
const defs=[['UG','Untergeschoss'],['EG','Erdgeschoss'],['OG1','1. Obergeschoss'],['OG2','2. Obergeschoss'],['Dach','Dach']],groups=new Map(),inputs=new Map();let center=new THREE.Vector3(),distance=55;
for(const [id,label] of defs){const row=document.createElement('label'),input=document.createElement('input');input.type='checkbox';input.checked=true;input.disabled=true;input.dataset.floor=id;row.append(input,document.createTextNode(label));document.querySelector('#floors').append(row);inputs.set(id,input);input.addEventListener('change',()=>{if(groups.has(id))groups.get(id).visible=input.checked;});}
function reset(){controls.target.copy(center);camera.position.copy(center).add(new THREE.Vector3(1,.72,1.15).normalize().multiplyScalar(distance*Math.max(1,.8/camera.aspect)));camera.up.set(0,1,0);controls.update();}
document.querySelector('#reset').onclick=reset;document.querySelector('#top').onclick=()=>{controls.target.copy(center);camera.position.copy(center).add(new THREE.Vector3(0,distance,.001));controls.update();};
function visibility(only){for(const [id,input] of inputs){input.checked=only===null||id===only;if(groups.has(id))groups.get(id).visible=input.checked;}}
document.querySelector('#all').onclick=()=>visibility(null);document.querySelector('#eg').onclick=()=>visibility('EG');document.querySelector('#toggle').onclick=()=>{const hidden=document.querySelector('#panel').classList.toggle('collapsed');document.querySelector('#toggle').setAttribute('aria-expanded',String(!hidden));};
new GLTFLoader().load('./model.glb',gltf=>{scene.add(gltf.scene);for(const [id] of defs){const group=gltf.scene.getObjectByName('Floor_'+id);if(!group)throw Error('Geschoss fehlt: '+id);groups.set(id,group);inputs.get(id).disabled=false;}const bounds=new THREE.Box3().setFromObject(gltf.scene);bounds.getCenter(center);const size=bounds.getSize(new THREE.Vector3());distance=Math.max(size.x,size.y,size.z)*2.05;reset();status.textContent='';window.viewerState={groups,scene,camera,renderer};},event=>{if(event.total)status.textContent=`Modell wird geladen … ${Math.round(event.loaded/event.total*100)} %`;},error=>{console.error(error);status.textContent='Das Modell konnte nicht geladen werden. Bitte die Seite neu laden.';});
addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);reset();});renderer.setAnimationLoop(()=>{controls.update();renderer.render(scene,camera);});
} catch(error){console.error(error);status.textContent='3D ist in diesem Browser nicht verfügbar. Bitte den Link in Safari, Chrome oder Firefox öffnen.';}
